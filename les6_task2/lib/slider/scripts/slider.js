let mySwiper = new Swiper('.swiper-container', {
    // Optional parameters
    lazy: true,
    loop: true,
 
    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  
  })